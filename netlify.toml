[build]
  publish = "public"
  command = "npm run build" # Agar tumhare frontend build command hai, warna hata do

# Edge Function mapping for xhttp proxy
[[edge_functions]]
  path = "/autovl"
  function = "proxy"

[[edge_functions]]
  path = "/autovl/*"
  function = "proxy"

# Optional headers (cache control)
[[headers]]
  for = "/autovl/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
